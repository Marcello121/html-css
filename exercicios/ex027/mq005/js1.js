function clickMenu(){

    // Se o style display do menu for igual a 'none', ou seja, se estiver escondido ele vai transformar o display em block, OU SEJA, vai mostrar o menu.
    if (menu.style.display == 'none'){
    
        menu.style.display = 'block';
    
    } else {
    menu.style.display = 'none';
    }

}